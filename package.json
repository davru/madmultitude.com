{
  "name": "madmultitude",
  "version": "0.0.1",
  "private": true,
  "description": "Mad Multitude e-commerce website",
  "scripts": {
    "dev": "pnpm --filter ./fe dev",
    "build": "pnpm --filter ./fe build",
    "preview": "pnpm --filter ./fe preview"
  },
  "packageManager": "pnpm@9.15.3",
  "engines": {
    "node": ">=20"
  }
}
